-- 코드를 입력하세요
SELECT NAME, COUNT (NAME) AS COUNT -- 중복된 이름과 해당 이름 쓰인 횟수 체크
FROM ANIMAL_INS
WHERE NAME IS NOT NULL -- 이름이 없는 동물 제외
GROUP BY NAME -- 중복된 이름인 동물들 묶기
HAVING COUNT >= 2 -- 2번 이상 쓰인 이름
ORDER BY NAME -- 이름 순 조회